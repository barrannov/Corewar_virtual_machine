#include "../../../includes/vm_header.h"

void set_cycles_live(char **map, t_processor *proc)
{
	proc->waite_cycles = 10;
}

void handle_live(char **map, t_processor *proc)
{
	proc->waite_cycles = 10;
}